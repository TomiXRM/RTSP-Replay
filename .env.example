# =============================================================================
# ロボット監視・リプレイシステム — 環境変数サンプル
# =============================================================================
# コピーして .env を作成し、値を書き換えてください。
#   cp .env.example .env
# .env はGit管理対象外です（認証情報をGitへコミットしない）。
#
# そのままコピーすればネイティブ実行（Docker不要版）で動きます。
# Docker 版を使う場合のみ「Docker版のみ」の行のコメントを外してください。
# =============================================================================

# -----------------------------------------------------------------------------
# カメラの RTSP 認証情報
# config/sources.toml の username_env / password_env が参照する変数名で設定する
# -----------------------------------------------------------------------------
CAM_USERNAME=
CAM_PASSWORD=

# -----------------------------------------------------------------------------
# 録画
# -----------------------------------------------------------------------------
# 録画保持時間（時間）。タイムライン表示幅・シーク範囲も連動。
# ⚙ ダイアログから保存すると再起動なしで反映される。
RECORDING_RETENTION_HOURS=2

# -----------------------------------------------------------------------------
# イベント受信
# -----------------------------------------------------------------------------
# UDP 受信ポート（0 で無効）。ROS 2 ブリッジ等からの送信先。
EVENT_UDP_PORT=8600

# -----------------------------------------------------------------------------
# ポート・URL（既定値のままなら変更不要）
# -----------------------------------------------------------------------------
#API_PORT=8000
#UI_PORT=5173
#MEDIAMTX_API=http://localhost:9997
#MEDIAMTX_HLS_BASE=http://localhost:8888
#MEDIAMTX_PLAYBACK_BASE=http://localhost:9996

# -----------------------------------------------------------------------------
# 保存先（未設定ならプロジェクト内 recordings/ と data/ を使用 — 推奨）
# -----------------------------------------------------------------------------
#RECORDINGS_DIR=
#INCIDENTS_DIR=
#DATABASE_PATH=

# -----------------------------------------------------------------------------
# Docker 版のみ（docker-compose 利用時にコメントを外す）
# -----------------------------------------------------------------------------
#MEDIAMTX_API=http://mediamtx:9997
#MEDIAMTX_HLS_BASE=http://mediamtx:8888
#RECORDINGS_DIR=/recordings
#INCIDENTS_DIR=/data/incidents
#DATABASE_PATH=/data/replay.db
